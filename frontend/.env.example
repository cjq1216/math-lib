# 前端环境配置
# 复制为 .env.local 后使用

# 留空 = 前端请求走 Next 同源代理 /api/v1/*（推荐，无跨域问题）
# 填写 = 前端直连后端，此时后端必须允许该来源的 CORS
# NEXT_PUBLIC_API_BASE=http://localhost:8000

# 服务端代理目标（next.config.ts 读取，本地开发默认 8000）
API_PROXY_TARGET=http://localhost:8000
